apiVersion: v1
kind: Service
metadata:
  name: sgac-web-test-service
spec:
  type: NodePort
  selector:
    app: sgac-web-test
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
